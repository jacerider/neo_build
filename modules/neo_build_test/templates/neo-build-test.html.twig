{{ attach_library('neo_build_test/front') }}
<div class="neo-build-test">
  <div class="neo-build-test--front p-2 mb-2 rounded bg-primary text-default">
    Neo Build test: this block is styled by the front library's CSS entrypoint
    and gains the class <code>neo-build-test--front-attached</code> from its TS
    entrypoint.
  </div>
</div>
